/**
 * @param {string} haystack
 * @param {string} needle
 * @return {number}
 */
var strStr = function(haystack, needle) {
    let solution =  haystack.indexOf(needle);
    console.log("solution", solution);
};

strStr(haystack = 'sadbutsad', needle = 'sad');